# PDFs dos livros-base de cada nível — não devem ir para o GitHub.
docs/*.pdf

# Pasta de arquivos marcados para exclusão manual (o Claude não consegue
# deletar arquivos neste ambiente, só mover) — pode apagar quando quiser.
_to_delete/
